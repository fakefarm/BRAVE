<h1>All of our users</h1>
<div class"users">
  <% @users.each do |user| %>
<ul> 
  <li><%= link_to user.name, user_url(user.id) %></li>
  <li><img width="50" class="photo" alt="dummypic" src="assets/pic.png"></li>
</ul>
  <% end %>
</div>