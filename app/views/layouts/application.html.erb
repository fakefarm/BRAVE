<!DOCTYPE html>
<html>
<head>
  <title>Brave</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
<link href='http://fonts.googleapis.com/css?family=Cabin+Condensed' rel='stylesheet' type='text/css'>
</head>
<body>
  <div class="headNav">      
    <% flash.each do |key, msg| %>
      <%= content_tag :div, msg, id: key %>
    <% end %>
    <a href="/"><%= image_tag "logo.png" %></a>
    <ul>
      <% if session[:uid] %>
        <li><%= link_to "Dashboard", user_url(current_user) %></li>
        <li class="last"><%= link_to "About", signout_url %></li>
        <li class="last"><%= link_to "Help", signout_url %></li>
        <li class="last"><%= link_to "Sign Out", signout_url %></li>		
        <li class="last">Hello, <%= current_user.name.capitalize %></li>
      <% else %>
        <li><%= link_to "Sign Up", new_user_url %> |</li>
        <li><%= link_to "Log In", new_session_path %></li>
      <% end %>
    </ul>
  </div><!-- .headNav -->
  <div class="container">
    <%= yield %>
  </div><!-- .container -->
</body>
